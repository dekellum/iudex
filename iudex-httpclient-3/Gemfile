# -*- ruby -*-
source :rubygems
gemspec :path => '.', :name => 'iudex-httpclient-3'

RJack::TarPit.last_spec.dependencies.each do |d|
  gem( d.name, :path => "../#{d.name}" ) if d.name =~ /^iudex/
end
